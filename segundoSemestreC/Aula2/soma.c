#include <stdio.h>

int main () {
    int num1, num2;

    num1 = 10;
    num2 = 25;

    soma = num1 + num2;
    sub = num2 - num1;
    vezes = num1 * num2;
    div = num2 / num1;


    printf ("A soma dos numeros eh igual a: %f \n", soma);
    printf ("A soma dos numeros eh igual a: %f \n", sub);
    printf ("A soma dos numeros eh igual a: %f \n", vezes);
    printf ("A soma dos numeros eh igual a: %f \n", div);
    
    return 0;
}
